# =============================================================================
# ───── src/Problems/main.jl

# The problem-class hierarchy. Included first by TrustRegionRadius.jl: the model
# Hessians, the sampling rules and all three solvers are checked against it.
# =============================================================================

include("classes.jl")