var documenterSearchIndex = {"docs":
[{"category":"section","location":"thresholds/#Thresholds-and-factors","page":"Thresholds and factors","text":"Two conventions govern every mechanism in the package. Both are enforced at construction, so a configuration that violates either fails immediately rather than producing a run that looks plausible and is outside the theory.","title":"Thresholds and factors"},{"category":"section","location":"thresholds/#Acceptance-is-not-scaling","page":"Thresholds and factors","text":"TRParams carries three thresholds satisfying\n\n0 le eta le eta_1 le eta_2  1 \n\nand they answer different questions:\n\nthreshold question read by\nη is this step worth taking? the solver alone\nη₁ was the model poor enough that the region should shrink? the rule alone\nη₂ was it good enough that the region should grow? the rule alone\n\nη defines the successful and unsuccessful index sets\n\nmathcalS = k  rho_k ge eta qquad mathcalU = k  rho_k  eta \n\nand nothing else in the package compares ρ with it. η₁ and η₂ never decide whether a step is taken.\n\nTRParams(η = 0.0, η₁ = 0.1, η₂ = 0.9)     # decoupled\nTRParams(η₁ = 0.1, η₂ = 0.9)              # η defaults to η₁: the classical algorithm\n\nBecause η defaults to η₁, existing code is unaffected until it asks for something else.","title":"Acceptance is not scaling"},{"category":"section","location":"thresholds/#What-decoupling-buys","page":"Thresholds and factors","text":"Setting η < η₁ opens a band in which ρ_k ∈ [η, η₁): the step is accepted and the radius still contracts. The model was good enough to make progress worth keeping and bad enough that the region should not be trusted at its current size. Nothing in the framework forbids this, and no rule needed changing to support it — the rules already branch on η₁.\n\nSetting η = 0 accepts every step with a positive predicted reduction. This is admissible in Part I and excluded by the framework of Curtis & Scheinberg (2020), which requires a strictly positive acceptance threshold; it is worth a column of its own in any comparison.\n\nnote: Count the band, not the shrinking radius\nTo measure how often the new regime is entered, test ρ_k < η₁ on accepted iterations — not whether Δ decreased. RDelta contracts by γ₂ on every mildly successful iteration, so a shrinking radius does not isolate the band:ss  = stats.solver_specific\nρ, acc = ss[:ratio_trajectory], ss[:accepted_trajectory]\ncount(i -> acc[i] && ρ[i] < params.η₁, eachindex(acc))The count is zero by construction whenever η = η₁.","title":"What decoupling buys"},{"category":"section","location":"thresholds/#Why-the-rules-are-told-the-outcome","page":"Thresholds and factors","text":"update_radius!(rule, Δ, ρ, accepted, η₁, η₂, s_norm, g_norm_old, g_norm_new)\n\naccepted is passed rather than recomputed. A rule cannot derive it: acceptance is decided by η, which the rule never receives, and the retrospective rules are handed ρ̃ in the ρ slot, so they do not see ρ_k at all. For RRTR and RRTRGrad this argument is load-bearing — see Retrospective rules and rejected steps.","title":"Why the rules are told the outcome"},{"category":"section","location":"thresholds/#Rules-that-constrain-the-thresholds","page":"Thresholds and factors","text":"Some rules cannot accept every admissible triple. The step-driven rules need η₁ > 0, because at η₁ = 0 their aggressive branch is unreachable and the lower-bound constant of Part I degenerates. validate_thresholds is called once when the solver is built, so the failure is a constructor error rather than a silent change of behaviour:\n\nTRSolver(nlp; rule = RStep(), params = TRParams(η = 0.0, η₁ = 0.0, η₂ = 0.9))\n# ArgumentError: RStep: needs η₁ > 0 …\n\nRules with no such requirement accept η₁ = 0 unchanged. To add a requirement for a new rule, define a method of validate_thresholds; the fallback accepts everything.","title":"Rules that constrain the thresholds"},{"category":"section","location":"thresholds/#One-convention-for-the-scaling-factors","page":"Thresholds and factors","text":"Every rule carries γ₁, γ₂, γ₃ with\n\n0  gamma_1 le gamma_2  1  gamma_3 \n\nγ₁ the aggressive contraction, γ₂ the mild one, γ₃ the expansion. This is the convention of Parts I–II, checked in every constructor by check_factors, which throws an ArgumentError naming the rule and the offending inequality. A rule that does not use one of the three passes nothing for it; the remaining inequalities are still checked.\n\nArgumentError rather than @assert deliberately: @assert is documented as liable to be disabled, and these are argument checks rather than internal invariants.\n\nRGrad(γ₂ = 2.0)                      # ArgumentError: γ₂ is the mild contraction\nRDelta(γ₁ = 0.8, γ₂ = 0.3)           # ArgumentError: need γ₁ ≤ γ₂\nRDelta(γ₃ = 0.9)                     # ArgumentError: need γ₃ > 1\nRGrad(γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0) # fine\n\nThe Hei family carries one extra requirement, γ₃ > 1 + γ₂, so that the smooth factor can exceed the value it takes at the threshold; see The Hei family.","title":"One convention for the scaling factors"},{"category":"section","location":"thresholds/#Rules-whose-factors-were-renumbered","page":"Thresholds and factors","text":"Four rules previously used γ₂ for expansion, which is now the mild contraction slot:\n\nrule before now\nRGrad γ₁ = 0.25, γ₂ = 2.0 γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0\nRGradCapped γ₁ = 0.25, γ₂ = 2.0 γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0\nRRTR γ₀ = 0.0625, γ₁ = 0.25, γ₂ = 2.5 γ₁ = 0.0625, γ₂ = 0.25, γ₃ = 2.5\nRRTRGrad γ₁ = 0.25, γ₂ = 2.0 γ₁ = 0.25, γ₃ = 2.0\n\nRDelta, RStep and RDFO keep their values; only their checks are tighter, since γ₂ < 1 and γ₁ ≤ γ₂ were previously unverified. Old keyword calls now throw, so none of these can pass silently. MIGRATION.md in the repository root lists the one case that can — a four-argument positional RGrad(...) written against the old signature.","title":"Rules whose factors were renumbered"},{"category":"section","location":"thresholds/#API","page":"Thresholds and factors","text":"","title":"API"},{"category":"function","location":"thresholds/#TrustRegionRadius.validate_thresholds","page":"Thresholds and factors","text":"validate_thresholds(rule, η, η₁, η₂) -> nothing\n\nHook called once per solve, after the parameters are known, for rules that require more of (η, η₁, η₂) than 0 ≤ η ≤ η₁ ≤ η₂ < 1. Default: no-op. Throws ArgumentError on a violation.\n\n\n\n\n\n","title":"TrustRegionRadius.validate_thresholds"},{"category":"function","location":"thresholds/#TrustRegionRadius.check_factors","page":"Thresholds and factors","text":"check_factors(name; γ₁, γ₂, γ₃) -> nothing\n\nEnforce the standing convention 0 < γ₁ ≤ γ₂ < 1 < γ₃ on the scaling factors of a rule. Pass nothing for a factor the rule does not use; the remaining inequalities are still checked.\n\nAn ArgumentError rather than @assert, because @assert is documented as liable to be disabled and these are argument checks, not internal invariants.\n\n\n\n\n\n","title":"TrustRegionRadius.check_factors"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Solver","page":"API reference","text":"","title":"Solver"},{"category":"section","location":"api/#Index","page":"API reference","text":"","title":"Index"},{"category":"type","location":"api/#TrustRegionRadius.TRParams","page":"API reference","text":"TRParams{T}(; η, η₁, η₂, Δ₀, Δmax, max_iterations, tol, max_time)\n\nSolver parameters, parametric on the element type so Float32 and BigFloat models keep their precision.\n\nFields\n\nη:   acceptance threshold; the step is taken when ρ ≥ η  (default η₁)\nη₁:  first scaling threshold, passed to the radius rule    (default 0.1)\nη₂:  second scaling threshold, \"very successful\"               (default 0.9)\nΔ₀:  initial radius; ignored by rules of the form Δ = μ‖g‖   (default 1)\nΔmax: hard cap on the radius                                  (default Inf)\nmax_iterations:                                               (default 10 000)\ntol: first-order tolerance on ‖g‖                           (default √eps)\nmax_time: wall-clock budget in seconds                        (default Inf)\n\nThe thresholds satisfy 0 ≤ η ≤ η₁ ≤ η₂ < 1.\n\nAcceptance is decoupled from scaling\n\nη governs whether the trial point is taken; η₁ and η₂ govern only how the radius is scaled. Taking η < η₁ opens a middle regime, ρ ∈ [η, η₁), in which the step is accepted and the radius nonetheless contracts — a combination the coupled formulation cannot express. η = 0 is admissible and accepts every non-increase in f; it is covered by the first-order framework of Part I but not by the Curtis-Scheinberg analysis, so it is worth a column of its own in any comparison.\n\nη defaults to η₁, which reproduces the coupled behaviour exactly. Pass η explicitly to decouple.\n\nnote: Hold these fixed across mechanisms\nIn any comparison the thresholds and factors must be identical for every rule. Tuning them per rule measures tuning effort rather than algorithmic merit and makes a performance profile uninterpretable. The one thing that may legitimately vary is η, because varying it is an experiment.\n\n\n\n\n\n","title":"TrustRegionRadius.TRParams"},{"category":"type","location":"api/#TrustRegionRadius.TRResult","page":"API reference","text":"TRResult\n\nThe result type: an alias of JSO's GenericExecutionStats, so anything in the JSO ecosystem (SolverBenchmark, solve!, callbacks) accepts it directly.\n\nUseful fields: status, solution, objective, dual_feas, iter, elapsed_time. Evaluation counts live on the model — neval_obj(nlp), neval_grad(nlp), neval_hprod(nlp) — not on the result.\n\nPer-iteration trajectories are attached under stats.solver_specific when trace = true; see tr_solve.\n\n\n\n\n\n","title":"TrustRegionRadius.TRResult"},{"category":"type","location":"api/#TrustRegionRadius.TRSolver","page":"API reference","text":"TRSolver{T, V, R, M, S} <: AbstractOptimizationSolver\n\nTrust-region solver with pre-allocated workspace.\n\nType parameters cover the element type T, vector type V, and the three axes R <: RadiusRule, M <: ModelHessian, S <: SubproblemSolver, so every dispatch in the loop is resolved at compile time.\n\nrule, model and subsolver are deep-copied at construction: all three may carry mutable state (μ, quasi-Newton operators), and the solver owns its copy so a single configuration object can be reused across problems.\n\n\n\n\n\n","title":"TrustRegionRadius.TRSolver"},{"category":"function","location":"api/#TrustRegionRadius.tr_solve","page":"API reference","text":"tr_solve(nlp; rule, model, subsolver, params, trace, callback) -> TRResult\n\nSolve nlp by the trust-region method. Constructs a TRSolver and runs it.\n\nusing TrustRegionRadius, ADNLPModels\n\nnlp = ADNLPModel(x -> (1 - x[1])^2 + 100(x[2] - x[1]^2)^2, [-1.2, 1.0])\n\nstats = tr_solve(nlp;\n    rule      = RGrad(),\n    model     = SR1Model(mem = 5),\n    subsolver = SteihaugCG(),\n    params    = TRParams(tol = 1e-8),\n    trace     = true)\n\nstats.status                                        # :first_order\nstats.solver_specific[:delta_trajectory]            # Δ per iteration\ncount(stats.solver_specific[:active_trajectory])    # iterations with ‖s‖ = Δ\n\n\n\n\n\n","title":"TrustRegionRadius.tr_solve"},{"category":"section","location":"subsolvers/#Subproblem-solvers","page":"Subproblem solvers","text":"All solve\n\nmin_s le Delta  g^top s + tfrac12 s^top H s\n\nand return active::Bool, true when ‖s‖ = Δ. That flag is worth propagating: the fraction of iterations on which the constraint binds is the observable that separates radius mechanisms whose first-order behaviour is identical.\n\nsolver needs handles indefinite H? size\nSteihaugCG B * v only yes, stops on negative curvature any\nExactMS dense eigendecomposition yes, including the hard case n ≤ nmax\nKrylovCG B * v no — assumes H ≻ 0 any\nKrylovCGLanczos B * v yes any","title":"Subproblem solvers"},{"category":"section","location":"subsolvers/#SteihaugCG-and-the-Cauchy-point","page":"Subproblem solvers","text":"The first CG direction is -g. A step that leaves the trust region on CG iteration 1 is therefore exactly the Cauchy point, and the model Hessian has had no influence on its direction whatsoever.\n\nThis is not a corner case. When the radius is small — a low μ_max in RGradCapped, a low ζ in RDFO — it happens at every iteration, and the method is gradient descent in disguise. Nothing in ρ, ‖g‖ or the radius trace reveals it. cg_step_info does:\n\ninfo = cg_step_info(SteihaugCG(), ExactHessian(), nlp, x, g, Δ)\ninfo.cg_iters     # 1\ninfo.active       # true\ninfo.cos_cauchy   # 1.0 to machine precision\n\ncg_iters == 1 && active together with cos_cauchy ≈ 1 certify the degeneration.","title":"SteihaugCG and the Cauchy point"},{"category":"section","location":"subsolvers/#ExactMS-and-the-hard-case","page":"Subproblem solvers","text":"Uses the Moré–Sorensen characterisation: s* is optimal iff there is λ ≥ 0 with\n\n(B + lambda I)s^* = -g qquad B + lambda I succeq 0 qquad lambda(Delta - s^*) = 0 \n\n‖s(λ)‖ decreases strictly in λ, so the root is bracketed and bisected.\n\nThe hard case — g orthogonal to the eigenspace of λ_min(B), where no λ attains ‖s(λ)‖ = Δ — is handled explicitly by adding the right multiple of that eigenvector. This matters with ExactHessian or SR1Model, where indefiniteness is exactly the information one wants to exploit; it cannot arise with a positive definite model, which is why solvers written for that case often omit it.\n\nRequires an eigendecomposition, hence n ≤ nmax (default 200). Larger problems raise rather than silently allocating an n × n array.","title":"ExactMS and the hard case"},{"category":"section","location":"subsolvers/#Adding-a-solver","page":"Subproblem solvers","text":"struct MySolver <: SubproblemSolver end\n\nfunction TrustRegionRadius.solve_subproblem!(::MySolver, model::ModelHessian, nlp,\n                                             x, g, Δ, s, Hbuf)\n    B = hessian_op(model, nlp, x)\n    # ... write the step into s ...\n    return norm(s) ≥ (1 - 1e-8) * Δ     # active?\nend\n\nTake the curvature from model, not from nlp: that is what lets one solver serve every model Hessian.","title":"Adding a solver"},{"category":"section","location":"subsolvers/#API","page":"Subproblem solvers","text":"","title":"API"},{"category":"type","location":"subsolvers/#TrustRegionRadius.SubproblemSolver","page":"Subproblem solvers","text":"SubproblemSolver\n\nAbstract supertype for trust-region subproblem solvers. A concrete subtype implements\n\nsolve_subproblem!(sub, model, nlp, x, g, Δ, s, Hbuf) -> active::Bool\n\nsolving min_{‖s‖ ≤ Δ} gᵀs + ½ sᵀHs and writing the step into s.\n\n\n\n\n\n","title":"TrustRegionRadius.SubproblemSolver"},{"category":"type","location":"subsolvers/#TrustRegionRadius.SteihaugCG","page":"Subproblem solvers","text":"SteihaugCG(; χ = 0.1, θ = 0.5, max_iters = 100)\n\nSteihaug–Toint truncated conjugate gradient. Stops on negative curvature, on reaching the boundary, or when the residual satisfies the forcing condition ‖r‖ ≤ min(χ, ‖g‖^θ)‖g‖.\n\nNeeds only B * v, so it scales to large problems with any operator-backed model.\n\nnote: The Cauchy point\nThe first CG direction is -g. A step that leaves the region on CG iteration 1 is therefore exactly the Cauchy point, and the model Hessian has had no influence on its direction. When the radius is small — a low μ_max in RGradCapped, a low ζ in RDFO — this happens at every iteration and the method is gradient descent in disguise. cg_iters in the return of cg_step_info reveals it.\n\n\n\n\n\n","title":"TrustRegionRadius.SteihaugCG"},{"category":"type","location":"subsolvers/#TrustRegionRadius.ExactMS","page":"Subproblem solvers","text":"ExactMS(; nmax = 200, tol = 1e-12, maxit = 100)\n\nExact step by the Moré–Sorensen characterisation: s* is optimal iff there is λ ≥ 0 with\n\n(B + λI)s* = −g,   B + λI ⪰ 0,   λ(Δ − ‖s*‖) = 0.\n\n‖s(λ)‖ decreases strictly in λ, so the root is bracketed and bisected.\n\nHandles indefinite B, including the hard case (g orthogonal to the eigenspace of λ_min(B), where no λ attains ‖s(λ)‖ = Δ), by adding the right multiple of that eigenvector. That matters with the exact Hessian or SR1, where indefiniteness is exactly the information one wants to exploit.\n\nRequires a dense eigendecomposition, hence n ≤ nmax; larger problems raise rather than silently allocating an n × n array.\n\n\n\n\n\n","title":"TrustRegionRadius.ExactMS"},{"category":"type","location":"subsolvers/#TrustRegionRadius.KrylovCG","page":"Subproblem solvers","text":"KrylovCG(; atol = 1e-6, rtol = 1e-6, itmax = 0)\n\nKrylov.cg with the radius keyword (Steihaug-style truncation). Assumes positive definite B; use KrylovCGLanczos otherwise.\n\n\n\n\n\n","title":"TrustRegionRadius.KrylovCG"},{"category":"type","location":"subsolvers/#TrustRegionRadius.KrylovCGLanczos","page":"Subproblem solvers","text":"KrylovCGLanczos(; atol = 1e-6, rtol = 1e-6, itmax = 0)\n\nKrylov.cg_lanczos with radius; handles indefinite B.\n\n\n\n\n\n","title":"TrustRegionRadius.KrylovCGLanczos"},{"category":"function","location":"subsolvers/#TrustRegionRadius.solve_subproblem!","page":"Subproblem solvers","text":"solve_subproblem!(sub, model, nlp, x, g, Δ, s, Hbuf) -> active::Bool\n\nSolve the trust-region subproblem, writing the step into s.\n\nsub:   the solver\nmodel: ModelHessian supplying the curvature\nnlp:   the problem, for hessian_op\nx, g:  current iterate and gradient\nΔ:     radius\ns:     output buffer for the step\nHbuf:  scratch for Hessian-vector products\n\nReturns true if ‖s‖ = Δ.\n\n\n\n\n\n","title":"TrustRegionRadius.solve_subproblem!"},{"category":"function","location":"subsolvers/#TrustRegionRadius.cg_step_info","page":"Subproblem solvers","text":"cg_step_info(sub, model, nlp, x, g, Δ) -> (; cg_iters, active, cos_cauchy)\n\nRun SteihaugCG and report how the step was produced:\n\ncg_iters:   CG iterations taken\nactive:     whether the step ended on the boundary\ncos_cauchy: cos(s, −g), which is 1 exactly when the step is the Cauchy               point\n\ncg_iters == 1 && active and cos_cauchy ≈ 1 together certify that the model Hessian played no part in choosing the direction. Allocating, and intended for diagnostics rather than the solver loop.\n\n\n\n\n\n","title":"TrustRegionRadius.cg_step_info"},{"category":"section","location":"benchmarking/#Benchmarking","page":"Benchmarking","text":"Two layers: profile functions in the package (pure functions of a cost matrix), and an experiment harness in benchmark/ that produces self-documenting archives.","title":"Benchmarking"},{"category":"section","location":"benchmarking/#Profiles","page":"Benchmarking","text":"T = [ 12.0  18.0  Inf ;      # rows: problems, columns: solvers\n      30.0  25.0  41.0 ]     # Inf marks a failure\n\nτ, prof = performance_profile(T)\n\nprof[1, s] is the fraction of problems on which solver s is fastest (efficiency); prof[end, s] is the fraction it solves at all (reliability). These move independently, and the distinction is the substance of most comparisons here — a parameter choice can leave efficiency untouched while collapsing reliability.\n\nnote: Ranking depends on the solver set\nRatios are normalised by the per-problem best, so adding or removing a solver can reorder the curves (Gould & Scott, 2016). Report pairwise profiles against a fixed baseline alongside the full comparison.\n\nwarning: Problems that cost nothing\nA problem already critical at x₀ returns :first_order with zero iterations. A cost of 0 is treated as a failure here, while success_table counts the same run as solved, so one such problem lowers every reliability asymptote and every median — and neither number says anything about a radius rule. Screen these out of the problem list before building the cost matrix, and log the names dropped. Several CUTEst *NE variants have a null objective, so ∇f ≡ 0 everywhere and every mechanism reports zero iterations.\n\ndata_profile is the complementary instrument: it reports the fraction solved within a budget measured in units of n+1 evaluations, is not normalised by the best solver, and so does not suffer that set-dependence.\n\nExport to pgfplots with profile_to_pgfplots:\n\nopen(io -> profile_to_pgfplots(io, τ, prof, labels), \"prof.tex\", \"w\")\n\n\\begin{axis}[xmode=log, xlabel={$\\tau$}, ylabel={$\\pi_s(\\tau)$}]\n  \\input{prof.tex}\n\\end{axis}","title":"Profiles"},{"category":"section","location":"benchmarking/#Run-matrix","page":"Benchmarking","text":"problems = [() -> ADNLPModel(f, x0, name = nm) for (nm, f, x0) in my_list]\nconfigs  = sweep_configs(\"ζ\", [0.01, 0.1, 1.0, 10.0], ζ -> RDFO(ζ = ζ))\n\nT, S = run_matrix(problems, configs; cost = :iter)\nsummarise(T, [c.label for c in configs])\n\nProblems are thunks, not models. This matters for CUTEst, where each CUTEstModel holds an open handle to a compiled SIF problem and only one may be live at a time; run_matrix finalises each before opening the next.\n\nOnly runs ending :first_order count as solved. A solver that stops early with a large gradient has not solved the problem, whatever its iteration count.\n\nRules and quasi-Newton models carry mutable state, so a sweep passes factories rather than instances: ζ -> RDFO(ζ = ζ) above, not a vector of built rules. Reusing one instance across problems makes the results depend on the order they were visited.","title":"Run matrix"},{"category":"section","location":"benchmarking/#The-experiment-suite","page":"Benchmarking","text":"julia --project=benchmark -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate()'\njulia --project=benchmark benchmark/experiments/run_all.jl\njulia --project=benchmark benchmark/experiments/run_all.jl 1 3 5   # a subset\n\nexperiment tests\nexp1_comparison like-for-like comparison; performance + data profiles\nexp2_trajectories Δ, ‖g‖, ρ per iteration; ΣΔ and ΣΔ² by family\nexp3_zeta_sweep the ζ threshold: reliability against efficiency\nexp4_mu_sweep the μ_max threshold; Cauchy-point diagnostic\nexp5_inactivity fraction of late iterations with ‖s‖ = Δ\nexp6_interaction rule × model grid; additivity residuals\nexp7_convergence_rate local order, conditioned on inactivity\nexp8_single_problem one problem in detail: Δ, ‖g‖, ρ and the inactivity countdown\n\nExperiment 8 takes a problem name, so it can be pointed anywhere without editing the script:\n\njulia --project=benchmark benchmark/experiments/exp8_single_problem.jl WOOD\nTRR_PROBLEM=ROSENBR julia --project=benchmark benchmark/experiments/exp8_single_problem.jl\n\nIt looks in the analytic set first and falls through to CUTEstModel, and writes one figure per quantity plus a stacked panel sharing an iteration axis. The countdown panel plots the number of active iterations still ahead of each index, so the onset of inactivity can be read against what Δ, ‖g‖ and ρ were doing at the time; a run whose staircase never reaches zero is labelled [never].\n\nEach run writes a self-documenting archive:\n\nbenchmark/results/exp_2026-04-16_02-08-34_zeta_sweep/\n├── experiment_config.toml     what was run\n├── experiment_summary.md      what came out\n├── figures/                   PDFs\n├── tables/                    text tables and .tex profile coordinates\n└── data/                      raw JLD2, one file per (problem, rule)\n\nRecover a past configuration with load_config(dir), or the most recent with latest_archive(). An interrupted campaign resumes in place:\n\nTRR_RESUME=benchmark/results/exp_2026-07-29_02-15-23_comparison \\\n  julia --project=benchmark benchmark/experiments/exp1_comparison.jl\n\nrun_experiment then skips any (problem, configuration) whose .jld2 already exists, and does not open a model at all when every configuration for that problem is cached — on CUTEst the SIF decode dominates the cost of a resume. See RESUME.md for the details.\n\nwarning: Experiment 7 conditions on inactivity\nThe convergence order is estimated only over the final run of inactive iterations. Including active ones mixes a linear phase with the asymptotic one and returns an order between the two — an artefact, not a finding. Configurations whose constraint never stops binding report never and no order; that is the result, not a gap in it.","title":"The experiment suite"},{"category":"section","location":"benchmarking/#API","page":"Benchmarking","text":"","title":"API"},{"category":"function","location":"benchmarking/#TrustRegionRadius.performance_profile","page":"Benchmarking","text":"performance_profile(T; ntau = 500, taumax = nothing) -> (τ, prof)\n\nPerformance profile of Dolan & Moré (2002).\n\nT[p, s] is the cost of solver s on problem p; use Inf or NaN for a failure. Returns the abscissa τ and the matrix\n\nprof[i, s] = fraction of problems with r_{p,s} ≤ τ[i],\nr_{p,s} = T[p,s] / min_{s'} T[p,s'].\n\nprof[1, s] is the fraction of problems on which s is fastest (efficiency); the right-hand asymptote is the fraction it solves at all (reliability).\n\nnote: Ranking depends on the solver set\nRatios are normalised by the per-problem best, so adding or removing a solver can reorder the curves (Gould & Scott 2016). Report pairwise profiles against a fixed baseline alongside the full comparison.\n\nExample\n\nτ, prof = performance_profile(T)\nopen(io -> profile_to_pgfplots(io, τ, prof, labels), \"prof.tex\", \"w\")\n\n\n\n\n\n","title":"TrustRegionRadius.performance_profile"},{"category":"function","location":"benchmarking/#TrustRegionRadius.data_profile","page":"Benchmarking","text":"data_profile(N, dims; nkappa = 500, kmax = nothing) -> (κ, d)\n\nData profile of Moré & Wild (2009).\n\nN[p, s] is the number of function evaluations solver s needed on problem p (Inf if unsolved) and dims[p] the dimension of problem p. The abscissa is the budget measured in units of n+1 evaluations — one simplex gradient — which is what makes problems of different size comparable.\n\nUnlike the performance profile, the data profile is not normalised by the best solver, so it does not suffer the set-dependence noted above; it answers \"what fraction is solved within this budget?\" directly.\n\n\n\n\n\n","title":"TrustRegionRadius.data_profile"},{"category":"function","location":"benchmarking/#TrustRegionRadius.profile_to_pgfplots","page":"Benchmarking","text":"profile_to_pgfplots(io, x, y, labels; npoints = 60, styles = nothing, logx = true)\n\nWrite \\addplot blocks ready to \\input into a pgfplots axis.\n\nThe curves are subsampled onto a logarithmic grid (or a linear one if logx = false, appropriate for data profiles) so the emitted file stays small: a profile computed on a 2000-point grid becomes ~60 coordinates per curve with no visible difference.\n\nstyles, if given, is a vector of pgfplots option strings, one per curve.\n\nExample\n\nτ, prof = performance_profile(T)\nopen(\"prof_mech.tex\", \"w\") do io\n    profile_to_pgfplots(io, τ, prof, [\"R-delta\", \"R-step\", \"R-DFO\", \"R-grad\"];\n                        styles = [\"black,solid\", \"black,dashed\",\n                                  \"gray,solid\", \"gray,dashdotted\"])\nend\n\nthen in LaTeX:\n\n\\begin{axis}[xmode=log, xlabel={$\\tau$}, ylabel={$\\pi_s(\\tau)$}]\n  \\input{prof_mech.tex}\n\\end{axis}\n\n\n\n\n\n","title":"TrustRegionRadius.profile_to_pgfplots"},{"category":"function","location":"benchmarking/#TrustRegionRadius.run_matrix","page":"Benchmarking","text":"run_matrix(problems, configs; cost = :iter, verbose = true)\n    -> (T, stats_matrix)\n\nRun every configuration on every problem.\n\nArguments\n\nproblems: vector of thunks, each returning a fresh AbstractNLPModel. Thunks rather than models so that CUTEst handles are opened and finalised around a single run.\nconfigs:  vector of TRConfig.\n\nKeyword arguments\n\ncost:    which quantity fills the matrix — :iter, :obj, :grad,            :hprod or :time.\nverbose: print one line per problem as it completes.\n\nReturns\n\nT[p, c]: the cost, or Inf where the run did not reach :first_order.\nstats_matrix[p, c]: the GenericExecutionStats, or nothing on error.\n\nOnly runs terminating with status :first_order are counted as solved; :max_iter, :user and any thrown error give Inf. That convention keeps the profile honest: a solver that stops early with a large gradient has not solved the problem, whatever its iteration count.\n\nExample\n\nproblems = [() -> ADNLPModel(x -> sum(x.^2), ones(5), name = \"sphere\")]\nconfigs  = [TRConfig(\"R-delta\"; rule = RDelta()),\n            TRConfig(\"R-grad\";  rule = RGrad())]\nT, S = run_matrix(problems, configs)\nsummarise(T, [c.label for c in configs])\n\n\n\n\n\n","title":"TrustRegionRadius.run_matrix"},{"category":"function","location":"benchmarking/#TrustRegionRadius.summarise","page":"Benchmarking","text":"summarise(T, labels; io = stdout)\n\nPrint solved counts, median and mean cost per configuration.\n\nThe median is over solved problems only, so it is not comparable across columns with very different reliability — read it together with the solved count, or use a performance profile, which handles the two jointly.\n\n\n\n\n\n","title":"TrustRegionRadius.summarise"},{"category":"type","location":"benchmarking/#TrustRegionRadius.TRConfig","page":"Benchmarking","text":"TRConfig(label; rule, model, subsolver, params)\n\nOne cell of the experiment matrix: a named triple of radius rule, model Hessian, and subproblem solver, together with the solver parameters.\n\nrule and model carry mutable state (μ for RGrad, the operator for L-BFGS/SR1), so run_matrix deep-copies them per run; a single TRConfig can therefore be reused across problems without contamination.\n\n\n\n\n\n","title":"TrustRegionRadius.TRConfig"},{"category":"function","location":"benchmarking/#TrustRegionRadius.sweep_configs","page":"Benchmarking","text":"sweep_configs(name, values, mkrule; model, subsolver, params) -> Vector{TRConfig}\n\nBuild one TRConfig per parameter value, labelled name = value.\n\nmkrule(v) must return a fresh rule for the value v.\n\nconfigs = sweep_configs(\"ζ\", [0.1, 1.0, 10.0], ζ -> RDFO(ζ = ζ))\nT, _    = run_matrix(problems, configs)\n\nA sweep of this kind is the right way to study ζ or μ_max: the survey's thresholds involve κ̄ = 4/λ*_min, which is a property of the solution and so cannot be chosen a priori, and only a sweep reveals where the transition sits on a given family of problems.\n\n\n\n\n\n","title":"TrustRegionRadius.sweep_configs"},{"category":"section","location":"quickstart/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"quickstart/#Installing","page":"Getting started","text":"using Pkg\nPkg.add(url = \"https://github.com/USER/TrustRegionRadius.jl\")\n\nFor development, clone and Pkg.develop(path = \".\").","title":"Installing"},{"category":"section","location":"quickstart/#A-first-solve","page":"Getting started","text":"using TrustRegionRadius, ADNLPModels\n\nrosen(x) = (1 - x[1])^2 + 100(x[2] - x[1]^2)^2\nnlp = ADNLPModel(rosen, [-1.2, 1.0])\n\nstats = tr_solve(nlp; rule = RDelta())\nstats.status      # :first_order\nstats.solution    # ≈ [1.0, 1.0]\nstats.iter\n\ntr_solve returns a TRResult, which is JSO's GenericExecutionStats, so SolverBenchmark and the rest of the ecosystem accept it unchanged. Evaluation counts live on the model — neval_obj(nlp), neval_grad(nlp), neval_hprod(nlp) — not on the result.","title":"A first solve"},{"category":"section","location":"quickstart/#Choosing-the-three-axes","page":"Getting started","text":"stats = tr_solve(nlp;\n    rule      = RGrad(μ = 1.0),      # how Δ moves\n    model     = SR1Model(mem = 5),   # what curvature the model reports\n    subsolver = SteihaugCG(),        # how the subproblem is solved\n    params    = TRParams(η = 0.1, η₁ = 0.1, η₂ = 0.9, Δ₀ = 1.0, tol = 1e-8))\n\nDefaults are RDelta(), ExactHessian(), SteihaugCG().\n\nnote: Hold `TRParams` fixed across mechanisms\nIn any comparison the thresholds and factors must be identical for every rule. Tuning them per rule measures tuning effort rather than algorithmic merit and makes a performance profile uninterpretable. The acceptance threshold η is the exception worth varying deliberately: it belongs to the algorithm rather than to any one mechanism, so changing it moves every column at once.","title":"Choosing the three axes"},{"category":"section","location":"quickstart/#The-three-thresholds","page":"Getting started","text":"η decides whether a step is accepted; η₁ and η₂ decide only how the radius is scaled, and are the only two a rule ever sees:\n\nTRParams(η₁ = 0.1, η₂ = 0.9)            # η defaults to η₁: the classical algorithm\nTRParams(η = 0.0, η₁ = 0.1, η₂ = 0.9)   # accept every step with positive predicted reduction\n\nThey must satisfy 0 ≤ η ≤ η₁ ≤ η₂ < 1, checked in the constructor. Setting η < η₁ opens a band where a step is accepted while the radius still contracts. Thresholds and factors covers the consequences, and the matching convention 0 < γ₁ ≤ γ₂ < 1 < γ₃ on the scaling factors.","title":"The three thresholds"},{"category":"section","location":"quickstart/#Tracing","page":"Getting started","text":"trace = true attaches per-iteration trajectories to stats.solver_specific:\n\nstats = tr_solve(nlp; rule = RGrad(), trace = true)\n\nss = stats.solver_specific\nss[:delta_trajectory]      # Δₖ            length k+1, starting at Δ₀\nss[:grad_trajectory]       # ‖gₖ‖          length k+1\nss[:obj_trajectory]        # f(xₖ)         length k+1\nss[:ratio_trajectory]      # ρₖ            length k\nss[:step_trajectory]       # ‖sₖ‖          length k\nss[:active_trajectory]     # ‖sₖ‖ == Δₖ    length k  (Bool)\nss[:accepted_trajectory]   # ρₖ ≥ η        length k  (Bool)\n\nThe first three are one entry longer than the rest, since they have a value before the first iteration; align on the tail when plotting them together.\n\nTwo are worth recording even when nothing else is. :active_trajectory decides whether the constraint eventually stops binding: two mechanisms can have identical iteration counts and identical first-order behaviour while one keeps the constraint permanently active and the other does not, and nothing else in a standard diagnostic distinguishes them. :accepted_trajectory cannot be reconstructed from :ratio_trajectory and η₁ once η < η₁, so it is the only record of which iterations belong to mathcalS.","title":"Tracing"},{"category":"section","location":"quickstart/#When-the-constraint-stopped-binding","page":"Getting started","text":"The tail fraction answers \"is it still binding at the end?\" but not \"when did it stop?\". For that, count the active iterations still ahead of each index:\n\na = ss[:active_trajectory]\nK = length(a)\nR = [count(@view a[k+1:end]) for k in 0:K-1]     # Rₖ, a staircase down to zero\n\nR starts at the total number of active iterations, steps down by one at each active iteration, and is flat on inactive ones. It reaches zero at the last active iteration k*, after which the step is the unconstrained model minimiser. The range stops at K-1 on purpose: R_K = 0 for every run, so including it would make every curve reach zero and erase the distinction. A staircase that never reaches zero was still binding on the final iteration, and the run never entered the regime where the local rate is available.\n\nBecause the run stops at ‖g‖ ≤ tol rather than at infinity, a positive tail is evidence of inactivity over the iterations observed, not a proof of eventual inactivity — and a tail of zero may mean only that the budget ran out first. Experiment 8 in benchmark/ plots this against Δ, ‖g‖ and ρ on a shared axis.","title":"When the constraint stopped binding"},{"category":"section","location":"quickstart/#Statuses","page":"Getting started","text":"status meaning\n:first_order ‖g‖ ≤ tol — the only status counted as solved in a profile\n:max_iter iteration budget exhausted\n:max_time wall-clock budget exhausted\n:stalled the step fell below the level at which f(x) − f(x+s) carries information, so ρ became noise\n:exception the model could not be built at the current iterate\n:user stopped by a callback\n\n:stalled deserves a word. Once the radius collapses far enough, the subtraction f(xₖ) − f(xₖ + sₖ) is pure rounding, ρ becomes noise, every step is rejected, and the radius contracts forever. This is a genuine numerical limitation of the criticality-anchored rules in floating point rather than a bug, and it is reported separately so that it is not silently folded into \"failure\".\n\nnote: `:first_order` at iteration 0\nA problem already critical at x₀ returns :first_order with iter == 0. That is correct but carries no information about any mechanism, and it reaches the two reporting paths inconsistently — a profile treats a cost of zero as a failure while a success table counts it as solved. Screen such problems out of a test set before building a cost matrix; several CUTEst *NE variants have a null objective and behave this way on every rule.","title":"Statuses"},{"category":"section","location":"quickstart/#Adding-your-own-rule","page":"Getting started","text":"One struct and two methods:\n\nstruct MyRule <: RadiusRule\n    γ₁::Float64\n    γ₂::Float64\n    γ₃::Float64\n    function MyRule(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0)\n        check_factors(:MyRule; γ₁ = γ₁, γ₂ = γ₂, γ₃ = γ₃)\n        new(γ₁, γ₂, γ₃)\n    end\nend\n\nTrustRegionRadius.initial_radius(::MyRule, Δ₀, g_norm) = Δ₀\n\nfunction TrustRegionRadius.update_radius!(r::MyRule, Δ::Float64, ρ::Float64, ::Bool,\n                                          η₁::Float64, η₂::Float64,\n                                          ::Float64, ::Float64, ::Float64)\n    ρ < η₁  && return r.γ₁ * Δ        # unsuccessful: the result MUST be < Δ\n    ρ ≥ η₂  && return r.γ₃ * Δ\n    return Δ\nend\n\nTrustRegionRadius.asymptotic_regime(::MyRule) = :bounded_below\n\nThen tr_solve(nlp; rule = MyRule()). update_radius! is exported, so extending it needs the qualified name; using brings it into scope without making it extensible. Arguments the rule ignores are written as bare type annotations.\n\nAdd reset_rule! if the struct carries mutable state, and needs_retrospective(::MyRule) = true if it should be driven by ρ̃ rather than ρ — the solver then computes the retrospective ratio, at the cost of one extra Hessian-vector product per accepted iteration. Add a validate_thresholds method if the rule cannot accept every admissible (η, η₁, η₂).","title":"Adding your own rule"},{"category":"section","location":"quickstart/#The-three-obligations","page":"Getting started","text":"Contract on unsuccessful iterations. If ρ < η₁ can leave Δ unchanged, the rule falls outside weak admissibility, the convergence theorem of Part I no longer covers it, and — since a rejected step changes neither the model nor the iterate — the solver re-solves an identical subproblem until the budget runs out. test/test_thresholds.jl checks this for every rule in the package at five values of ρ below η; add new rules to it.\n\nReturn a strictly positive radius. A rule anchored to ‖sₖ‖ can collapse to zero. That is what a Δmin field is for.\n\nDeclare the regime. asymptotic_regime and, where they apply, needs_retrospective and is_criticality_anchored are what the experiment scripts group by.","title":"The three obligations"},{"category":"section","location":"quickstart/#The-arguments-in-order","page":"Getting started","text":"update_radius!(rule, Δ, ρ, accepted, η₁, η₂, s_norm, g_norm_old, g_norm_new)\n\naccepted is third. It is passed rather than recomputed because a rule cannot derive it: acceptance is decided by η, which the rule never receives, and a retrospective rule is handed ρ̃ in the ρ slot and so never sees ρ at all.\n\nNote the two gradient-norm arguments: g_norm_old is ‖gₖ‖ before the accept/reject decision and g_norm_new is ‖gₖ₊₁‖ after it. Rules anchored to criticality at the current iterate (RDFO) use the former; rules that set the next radius from the next gradient (RGrad and relatives) use the latter. Getting this backwards is the easiest mistake to make here, and it shifts every threshold silently.","title":"The arguments in order"},{"category":"section","location":"models/#Model-Hessians","page":"Model Hessians","text":"The solver never forms a matrix: it needs only the operator action H·v, which is what hessian_op returns. This keeps large problems feasible and lets quasi-Newton models be supplied as LinearOperators without changing the solver loop.\n\nmodel curvature can be indefinite?\nExactHessian ∇²f(xₖ) yes\nLBFGSModel limited-memory BFGS no — enforced positive definite\nSR1Model symmetric rank one yes, and that is the point\nScaledIdentity c·I, i.e. none no\nSPDTarget constructed, minimiser pinned no — positive definite by design","title":"Model Hessians"},{"category":"section","location":"models/#Which-to-use","page":"Model Hessians","text":"ExactHessian for small problems and for any experiment where the model should not be a confounder. Note it does not satisfy the secant equation, so the Fan–Pan–Song route to ρ̃ → 1 does not apply to it; the Bastin route, which needs asymptotic second-order coherence instead, does.\n\nLBFGSModel never reports negative curvature. On a problem whose true Hessian is indefinite along the trajectory this is invisible to every first-order diagnostic: ρ stays healthy, ‖g‖ decreases, and the limit can still fail second-order optimality.\n\nSR1Model may be indefinite, which is what lets a subsolver that exploits negative curvature escape a saddle a positive-definite model would converge to. Pair it with SteihaugCG (which detects dᵀHd ≤ 0) or ExactMS (which handles the hard case).\n\nScaledIdentity is a diagnostic instrument, not a practical model: with it the step is -g/c truncated to the region, so the method is exactly gradient descent and any difference between radius rules is attributable to the rule alone.","title":"Which to use"},{"category":"section","location":"models/#SPDTarget","page":"Model Hessians","text":"A two-dimensional construction whose unconstrained model minimiser is pinned to target while H ≻ 0. With d = target − x, u = d/‖d‖, v = u^⊥:\n\nH = auu^top + b(uv^top + vu^top) + cvv^top\nqquad a = -fracg^top udquad b = -fracg^top vdquad c = fracb^2a + lambda_perp \n\nThis has minimiser target for every c, and is positive definite exactly when a > 0, equivalently when\n\nvarphi(x) = g^top(texttarget - x)  0 \n\nthat is, the target lies downhill. That condition is necessary for any positive definite model with that minimiser, so a DomainError is not an artefact of the construction: no such model exists at x. Query it directly with phi_target.\n\nPoint a SPDTarget at a saddle to obtain a run in which every hypothesis of the first-order theory holds, every ρ is successful, ‖g‖ → 0 — and the limit is a saddle. The failure is the model, and no radius rule can repair it.\n\nnlp = ADNLPModel(p -> p[1]^4 - p[1]^3 + (0.25 - p[1]/2)*p[2]^2 + p[2]^4/4, [-0.5, 0.6])\nstats = tr_solve(nlp; rule = RDelta(), model = SPDTarget(target = [0.75, 0.0]))\nstats.solution     # ≈ [0.75, 0.0] — the saddle","title":"SPDTarget"},{"category":"section","location":"models/#Adding-a-model","page":"Model Hessians","text":"struct MyModel <: ModelHessian end\n\nTrustRegionRadius.hessian_op(::MyModel, nlp, x) = # anything supporting B * v\nTrustRegionRadius.dense_hessian(::MyModel, nlp, x) = # Matrix, for ExactMS and diagnostics\n\nAdd reset_model! and update_model! if the model carries state; both default to no-ops. update_model! receives the secant pair (sₖ, yₖ = gₖ₊₁ − gₖ) on accepted steps only.","title":"Adding a model"},{"category":"section","location":"models/#API","page":"Model Hessians","text":"","title":"API"},{"category":"type","location":"models/#TrustRegionRadius.ModelHessian","page":"Model Hessians","text":"ModelHessian\n\nAbstract supertype for the model Hessian H_k used in the trust-region model\n\nm_k(s) = f(x_k) + g_kᵀ s + ½ sᵀ H_k s.\n\nSee ExactHessian, LBFGSModel, SR1Model, ScaledIdentity, SPDTarget.\n\n\n\n\n\n","title":"TrustRegionRadius.ModelHessian"},{"category":"type","location":"models/#TrustRegionRadius.ExactHessian","page":"Model Hessians","text":"ExactHessian()\n\nThe true Hessian ∇²f(x_k), as an operator via NLPModels.hess_op.\n\nNote that the exact Hessian does not satisfy the secant equation H_{k+1} s_k = y_k, so retrospective rules relying on that condition (the Fan–Pan–Song route to ρ̃ → 1) do not apply to it; the Bastin route, which needs asymptotic second-order coherence instead, does.\n\n\n\n\n\n","title":"TrustRegionRadius.ExactHessian"},{"category":"type","location":"models/#TrustRegionRadius.LBFGSModel","page":"Model Hessians","text":"LBFGSModel(; mem = 5)\n\nLimited-memory BFGS model Hessian, backed by LinearOperators.LBFGSOperator.\n\nThe operator enforces positive definiteness, so the model never reports negative curvature. On a problem whose true Hessian is indefinite along the trajectory this is invisible to every first-order diagnostic: ρ stays healthy, ‖g‖ decreases, and the limit can still fail second-order optimality.\n\n\n\n\n\n","title":"TrustRegionRadius.LBFGSModel"},{"category":"type","location":"models/#TrustRegionRadius.SR1Model","page":"Model Hessians","text":"SR1Model(; mem = 5)\n\nSymmetric rank-one model Hessian, backed by LinearOperators.LSR1Operator.\n\nUnlike L-BFGS, SR1 may be indefinite — which is the point: it can represent negative curvature, so a subsolver that exploits it (truncated CG detecting dᵀHd ≤ 0, or an exact solver handling the hard case) can escape a saddle that a positive-definite model would converge to.\n\n\n\n\n\n","title":"TrustRegionRadius.SR1Model"},{"category":"type","location":"models/#TrustRegionRadius.ScaledIdentity","page":"Model Hessians","text":"ScaledIdentity(; c = 1.0)\n\nH_k = c·I. Carries no curvature information whatsoever.\n\nDiagnostic instrument rather than a practical model: with this model the trust-region step is -g/c truncated to the region, so the method is exactly gradient descent and any difference between radius rules is attributable to the radius rule alone. Useful for isolating the Cauchy-point degeneration described in the survey.\n\n\n\n\n\n","title":"TrustRegionRadius.ScaledIdentity"},{"category":"type","location":"models/#TrustRegionRadius.SPDTarget","page":"Model Hessians","text":"SPDTarget(; target, λ⊥ = 1.0)\n\nTwo-dimensional construction whose unconstrained model minimiser is pinned to target, while H_k ≻ 0.\n\nWith d = target - x, u = d/‖d‖, v = u^⊥:\n\nH = a·uuᵀ + b·(uvᵀ + vuᵀ) + c·vvᵀ,\na = -gᵀu/‖d‖,   b = -gᵀv/‖d‖,   c = b²/a + λ⊥.\n\nThis has minimiser target for every c, and is positive definite exactly when a > 0, equivalently when\n\nφ(x) = gᵀ(target - x) < 0,\n\ni.e. the target lies downhill. That condition is necessary for any SPD model with that minimiser, so a DomainError here is not an artefact of the construction: no such model exists at x.\n\nPoint a SPDTarget at a saddle to obtain a run in which every hypothesis of the first-order theory holds, every ρ is successful, ‖g‖ → 0, and the limit is nonetheless a saddle — the failure being the model, which no radius rule can repair.\n\n\n\n\n\n","title":"TrustRegionRadius.SPDTarget"},{"category":"function","location":"models/#TrustRegionRadius.hessian_op","page":"Model Hessians","text":"hessian_op(model, nlp, x)\n\nReturn an object B supporting B * v, representing H_k at x.\n\n\n\n\n\n","title":"TrustRegionRadius.hessian_op"},{"category":"function","location":"models/#TrustRegionRadius.dense_hessian","page":"Model Hessians","text":"dense_hessian(model, nlp, x) -> Matrix\n\nDense representation, for diagnostics on small problems only.\n\n\n\n\n\n","title":"TrustRegionRadius.dense_hessian"},{"category":"function","location":"models/#TrustRegionRadius.model_hprod!","page":"Model Hessians","text":"model_hprod!(model, nlp, x, v, Hv)\n\nWrite H_k · v into Hv, where H_k is the model Hessian.\n\nUsed for the predicted reduction -gᵀs - ½ sᵀH_k s. Using ∇²f here instead would make ρ measure agreement between the true function and a model the algorithm never minimised, so ρ would stop being the quantity the acceptance test and the radius rules are stated in terms of.\n\n\n\n\n\n","title":"TrustRegionRadius.model_hprod!"},{"category":"function","location":"models/#TrustRegionRadius.update_model!","page":"Model Hessians","text":"update_model!(model, s, y)\n\nAbsorb the secant pair (s_k, y_k) with y_k = g_{k+1} - g_k, on accepted steps only. Default: no-op.\n\n\n\n\n\n","title":"TrustRegionRadius.update_model!"},{"category":"function","location":"models/#TrustRegionRadius.reset_model!","page":"Model Hessians","text":"reset_model!(model, n)\n\nClear any mutable state, for a problem of dimension n. Called at the start of every solve. Default: no-op.\n\n\n\n\n\n","title":"TrustRegionRadius.reset_model!"},{"category":"function","location":"models/#TrustRegionRadius.phi_target","page":"Model Hessians","text":"phi_target(model, nlp, x) -> Float64\n\nThe descent functional φ(x) = ∇f(x)ᵀ(target − x). The SPDTarget construction exists at x if and only if φ(x) < 0.\n\n\n\n\n\n","title":"TrustRegionRadius.phi_target"},{"category":"section","location":"#TrustRegionRadius.jl","page":"Home","text":"A testbed for trust-region radius update mechanisms, built on the JuliaSmoothOptimizers stack.\n\nCompanion code for A survey of trust-region radius update mechanisms, Parts I–III.","title":"TrustRegionRadius.jl"},{"category":"section","location":"#Why-this-package-exists","page":"Home","text":"Most trust-region codes fix one radius rule and vary everything else. This package does the opposite: the radius rule is a first-class, swappable component, and so are the two things it interacts with. Three orthogonal axes:\n\naxis choices\nradius rule RDelta, RStep, RDFO, RGrad, RGradCapped, RAdaptiveStep, RAdaptiveGrad, RAdaptiveFanYuan, RRTR, RRTRGrad\nmodel Hessian ExactHessian, LBFGSModel, SR1Model, ScaledIdentity, SPDTarget\nsubproblem solver SteihaugCG, ExactMS, KrylovCG, KrylovCGLanczos\n\nEvery combination runs through one driver, so a comparison measures the axis you varied rather than the difference between two code bases. That single-driver design is the main methodological control behind the numerical results in Part III.","title":"Why this package exists"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/USER/TrustRegionRadius.jl\")","title":"Installation"},{"category":"section","location":"#Thirty-seconds","page":"Home","text":"using TrustRegionRadius, ADNLPModels\n\nnlp = ADNLPModel(x -> (1 - x[1])^2 + 100(x[2] - x[1]^2)^2, [-1.2, 1.0])\n\nstats = tr_solve(nlp;\n    rule      = RGrad(),\n    model     = SR1Model(mem = 5),\n    subsolver = SteihaugCG(),\n    params    = TRParams(tol = 1e-8),\n    trace     = true)\n\nstats.status                                       # :first_order\ncount(stats.solver_specific[:active_trajectory])   # iterations with ‖s‖ = Δ\n\nContinue with Getting started.","title":"Thirty seconds"},{"category":"section","location":"#Three-results-worth-knowing-before-you-choose-a-rule","page":"Home","text":"RGrad is uncapped; RGradCapped is not. For RGrad the multiplier mu_k is exactly the ratio Delta_kg_k, and it grows geometrically past any threshold, so the trust-region constraint eventually stops binding with no side condition. RGradCapped supplies the bound the asymptotic theory assumes, at the cost of requiring barmu  barkappa = 4lambda^*_min — a constant that depends on the solution and so cannot be chosen in advance. Below that threshold the constraint binds forever and the method degrades to linear convergence while every first-order diagnostic looks healthy.\n\nThe activity flag is the observable that matters. Whether the constraint eventually stops binding separates mechanisms whose first-order behaviour is indistinguishable: both groups drive g_k to 0. Pass trace = true and read :active_trajectory; for when it stopped rather than whether, count the active iterations still ahead of each index, as in Getting started.\n\nAcceptance and scaling answer different questions. eta decides whether a step is worth taking; eta_1 and eta_2 decide only whether the region should shrink or grow, and are the only two any rule sees. Coupling them, as the classical statement does, hides the fact that eta = 0 is admissible and that rho_k in eta eta_1) accepts a step while still contracting the radius. See Thresholds and factors.","title":"Three results worth knowing before you choose a rule"},{"category":"section","location":"#Interface-changes","page":"Home","text":"Acceptance is decoupled from scaling, update_radius! takes an accepted::Bool third argument, and every rule obeys 0  gamma_1 le gamma_2  1  gamma_3. Because eta defaults to eta_1, existing TRParams calls are unaffected; four rules had their factors renumbered and will now throw rather than be silently reinterpreted. Thresholds and factors explains both conventions, and MIGRATION.md in the repository root lists what to edit.","title":"Interface changes"},{"category":"section","location":"#Citing","page":"Home","text":"See CITATION.cff, or cite Part I of the survey directly.","title":"Citing"},{"category":"section","location":"rules/#Radius-mechanisms","page":"Radius mechanisms","text":"Ten rules, all concrete subtypes of RadiusRule. Every one reads the scaling thresholds η₁ and η₂ and none reads the acceptance threshold η; see Thresholds and factors.","title":"Radius mechanisms"},{"category":"section","location":"rules/#Choosing-one","page":"Radius mechanisms","text":"rule anchored to regime needs a parameter above κ̄?\nRDelta nothing :bounded_below no\nRStep ‖sₖ‖ :step_summable no\nRDFO ‖gₖ‖ via ζ :vanishing yes (ζ > κ̄)\nRGrad ‖gₖ‖ via μₖ, uncapped :vanishing no\nRGradCapped ‖gₖ‖ via μₖ ≤ μ_max :vanishing yes (μ_max > κ̄)\nRAdaptiveStep ‖sₖ‖, smooth factor :step_summable no\nRAdaptiveGrad ‖gₖ₊₁‖ via μₖ :vanishing no\nRRTR ‖sₖ‖, driven by ρ̃ :step_summable no\nRRTRGrad ‖gₖ₊₁‖ via μₖ, driven by ρ̃ :vanishing no\n\nκ̄ = 4/λ*_min(∇²f(x*)) is a property of the solution, so a rule whose guarantee depends on exceeding it cannot be configured reliably in advance. That is the single most useful fact in the table.\n\nRAdaptiveGrad accumulates instead, and so climbs past any threshold as RGrad does.","title":"Choosing one"},{"category":"section","location":"rules/#The-three-regimes","page":"Radius mechanisms","text":"asymptotic_regime reports which of the three regimes of Part II §3 a rule belongs to. The older two-valued is_criticality_anchored cannot express the middle case, which is the one that separates RStep from RDelta:\n\nregime meaning rules\n:vanishing Δₖ → 0 with Σ Δₖ²/Mₖ < ∞, from the rule alone RDFO, RGrad, RGradCapped, RAdaptiveGrad, RRTRGrad\n:step_summable Δₖ inherits the summability of Σ‖sₖ‖ in the local regime, so Δₖ → 0 there and nothing follows globally RStep, RAdaptiveStep, RRTR\n:bounded_below liminf Δₖ > 0 RDelta","title":"The three regimes"},{"category":"section","location":"rules/#The-families","page":"Radius mechanisms","text":"Criticality-blind (RDelta). The radius depends on its own history and on ρ, never on ‖gₖ‖ or ‖sₖ‖. Consequently liminf Δₖ > 0: the constraint eventually stops binding with no side condition, but the radius carries no asymptotic information about criticality.\n\nStep-driven (RStep, RAdaptiveStep, RRTR). The radius tracks the trial step. All carry a Δmin floor, and it is not cosmetic — see the warning below. All require η₁ > 0, which validate_thresholds enforces when the solver is built.\n\nCriticality-anchored (RDFO, RGrad, RGradCapped,  RAdaptiveGrad, RRTRGrad). The radius is tied to ‖gₖ‖, so Δₖ → 0. These are the rules for which the κ̄ threshold matters. Query with is_criticality_anchored.\n\nRetrospective (RRTR, RRTRGrad). The radius update is judged by ρ̃, computed from the new model at the previous iterate, rather than by ρ. Query with needs_retrospective.\n\nwarning: `Δmin` on the step-driven rules\nOn an accepted step the new radius is proportional to ‖sₖ‖, so a short step gives a small radius, which gives a shorter step still. Near convergence — or when a truncated-CG solver stops on its first iteration — this can collapse to zero, after which every step is zero, ρ is NaN, and the solver spins to max_iterations. Δmin (default 1e-14) floors the radius and turns that silent hang into ordinary slow progress. Set Δmin = 0.0 to reproduce the unguarded rule exactly, which is the form the asymptotic claims of Part II are stated for.","title":"The families"},{"category":"section","location":"rules/#Retrospective-rules-and-rejected-steps","page":"Radius mechanisms","text":"ρ̃ compares the new model against f at the previous iterate, so it is defined only once the model has moved. On a rejected step there is no new model, and the solver passes ρₖ in the ρ̃ slot. Both retrospective rules therefore branch on accepted before comparing anything with their own thresholds η̃₁, η̃₂.\n\nRRTR falls back to the step-anchored contraction of Part I:\n\nDelta_k+1 =\nbegincases\nmingamma_2s_k gamma_1Delta_k  rho_k  0 \ngamma_2s_k  0 le rho_k  eta \nendcases\n\nand RRTRGrad contracts its multiplier, μ ← γ₁μ. The γ₁ slot of RRTR plays the role of the safeguard factor max[γ₁, θₖ] of Part I, held at a constant here.\n\ndanger: Why the branch order matters\nComparing a classical ρ against the retrospective thresholds can return Δₖ₊₁ = Δₖ after a rejection. Since a rejected step changes neither the model nor the iterate, the next iteration then re-solves an identical subproblem, gets an identical rejection, and the run continues to max_iterations making no progress. It also violates the contraction condition, so the convergence theorem of Part I does not cover it. A rule added outside the package must contract on every unsuccessful iteration; test/test_thresholds.jl checks this for all ten rules at five values of ρ below η.\n\nOn an accepted step the first branch is the interesting one, max(γ₃‖sₖ‖, Δₖ): simultaneously step-driven and non-decreasing, which is what lets RRTR secure eventual inactivity from a consequence of the standing assumptions (ρ̃ → 1) rather than from a condition on a user parameter, as RDFO needs. The price is structural: ρ̃ → 1 requires either the secant condition on the model, or asymptotic second-order coherence together with a quadratic model decrease.","title":"Retrospective rules and rejected steps"},{"category":"section","location":"rules/#RGrad-versus-RGradCapped","page":"Radius mechanisms","text":"μₖ is the radius-to-criticality ratio Δₖ/‖gₖ‖, so RGrad performs a geometric search for the inactivity threshold κ̄ without knowing it: μ climbs until the constraint stops binding, then stops climbing. Because it is uncapped it crosses any threshold eventually, which makes eventual inactivity unconditional — the only rule in the survey for which this holds without a hypothesis on an unknowable constant.\n\nBoth take the four-branch form of Part I:\n\nmu_k+1 =\nbegincases\ngamma_1mu_k  rho_k  eta_1 \ngamma_2mu_k  eta_1 le rho_k  eta_2 \ngamma_3mu_k  rho_k ge eta_2  textand s_k  tfrac12Delta_k \nmu_k  textotherwise \nendcases\n\nThe second branch contracts μ on mildly successful iterations, so the climb is not monotone. contract_middle = false holds μ fixed there instead; the boundedness argument for μ is stated for the four-branch form, so the default keeps it.\n\nRGradCapped supplies the bound μₖ ≤ μ_max that the global asymptotic theory assumes (Δₖ → 0 needs it). The cost is real: with a truncated-CG subsolver a small μ_max makes CG truncate on its first iteration, so the returned step is the Cauchy point and the model Hessian stops influencing the search direction at all. The method has silently become gradient descent, and with a quasi-Newton model that can change which critical point is reached. Diagnose with cg_step_info.\n\nThe guard ‖sₖ‖ > ½Δₖ is not cosmetic either: it is what converts \"μ grows\" into \"‖sₖ‖ is comparable to μₖ‖gₖ‖\", which is the step that makes the boundedness argument for μ work. Any constant in (0,1) serves; half_test = false disables the guard and breaks the argument.","title":"RGrad versus RGradCapped"},{"category":"section","location":"rules/#The-Hei-family","page":"Radius mechanisms","text":"RAdaptiveStep and RAdaptiveGrad scale by a smooth factor R_{η₁}(ρₖ) instead of one of three constants. The factor is parameterised by the same γ₁, γ₂, γ₃ as every other rule, plus two shape rates λ₁, λ₂ > 0:\n\nR_eta_1(t) =\nbegincases\ngamma_1 + (gamma_2 - gamma_1)e^lambda_1(t-eta_1)  t  eta_1 \n(1+gamma_2) + bigl(gamma_3 - 1 - gamma_2bigr)bigl(1 - e^-lambda_2(t-eta_1)bigr)  t ge eta_1 \nendcases\n\nso that R(−∞) = γ₁, R(t) < γ₂ below the threshold, R(η₁) = 1 + γ₂, and R(+∞) = γ₃. Hence the extra requirement γ₃ > 1 + γ₂. Only the limits and the value at η₁ matter for admissibility, so λ₁ and λ₂ are free.\n\nThe jump at η₁, from γ₂ < 1 to 1 + γ₂ > 1, is required by those conditions rather than an artefact: no continuous function satisfies both R ≤ γ₂ below the threshold and R(η₁) = 1 + γ₂ at it.\n\nThe kink sits at the solver's η₁ rather than at a threshold stored on the rule, so the family is directly comparable with the three-case rules under one (η, η₁, η₂).","title":"The Hei family"},{"category":"section","location":"rules/#API","page":"Radius mechanisms","text":"","title":"API"},{"category":"type","location":"rules/#TrustRegionRadius.RadiusRule","page":"Radius mechanisms","text":"RadiusRule\n\nAbstract supertype for trust-region radius update mechanisms.\n\nA concrete MyRule <: RadiusRule must implement\n\ninitial_radius(rule, Δ₀, g_norm) -> Float64\nupdate_radius!(rule, Δ, ρ, accepted, η₁, η₂,\n               s_norm, g_norm_old, g_norm_new) -> Float64\n\nand, if it carries mutable state,\n\nreset_rule!(rule) -> nothing\n\nArguments of update_radius!\n\nΔ:        the current radius Δ_k.\nρ:        the ratio that drives the scaling. This is ρk for ordinary             rules and the retrospective ratio ρ̃k for those declaring             needs_retrospective. On a rejected step ρ̃ is undefined             and the solver passes ρ_k, so a retrospective rule must branch on             accepted before it compares ρ to its own thresholds.\naccepted: whether the step was taken, i.e. ρ_k ≥ η. Decided by the solver             with the acceptance threshold η, which the rule never sees.\nη₁, η₂:   scaling thresholds, η ≤ η₁ ≤ η₂ < 1.\ns_norm:   ‖s_k‖, the realised trial step length.\ng_norm_old: ‖g_k‖, before the accept/reject decision. Used by rules             anchored to criticality at the current iterate (RDFO).\ng_norm_new: ‖g{k+1}‖, after it. Used by rules of the form Δ = μ‖g‖             (RGrad and relatives). Equal to `gnorm_old` on a             rejected step.\n\nEvery rule must contract when the iteration is unsuccessful — Δ_{k+1} ≤ γ₂Δ_k whenever ρ_k < η — or it forfeits weak admissibility, and, since a rejected step leaves both the model and the iterate unchanged, a rule that returns Δ_{k+1} = Δ_k there will re-solve an identical subproblem for ever. Because η ≤ η₁, branching on ρ < η₁ is enough to guarantee it, which is why most rules can ignore accepted.\n\n\n\n\n\n","title":"TrustRegionRadius.RadiusRule"},{"category":"type","location":"rules/#TrustRegionRadius.RDelta","page":"Radius mechanisms","text":"RDelta(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0, Δmax = Inf)\n\nClassical three-case multiplicative update (Conn, Gould & Toint §6.1):\n\nρ < η₁       →  Δ ← γ₁ Δ\nη₁ ≤ ρ < η₂  →  Δ ← γ₂ Δ\nρ ≥ η₂       →  Δ ← γ₃ Δ\n\nThe branches are read off ρ alone, never off acceptance, so with η < η₁ this rule contracts on iterations whose step was nonetheless taken — which is the whole content of decoupling the two thresholds.\n\nCriticality-blind: the radius depends on its own history and on ρ, never on ‖g‖. Consequently liminf Δ_k > 0, the trust-region constraint eventually stops binding with no side condition, and the radius carries no asymptotic information about criticality.\n\n\n\n\n\n","title":"TrustRegionRadius.RDelta"},{"category":"type","location":"rules/#TrustRegionRadius.RStep","page":"Radius mechanisms","text":"RStep(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0, Δmax = Inf, Δmin = 1e-14,\n        contract_on_step = true)\n\nRadius proportional to the realised step:\n\nρ < η₁       →  Δ ← γ₁ ‖s_k‖     (or γ₁ Δ_k, see `contract_on_step`)\nη₁ ≤ ρ < η₂  →  Δ ← γ₂ ‖s_k‖\nρ ≥ η₂       →  Δ ← γ₃ ‖s_k‖\n\ncontract_on_step = true is the rule as stated in Part I, in which all three branches are anchored to ‖s_k‖. false gives the variant that contracts on the previous radius instead, which is milder whenever the subsolver returned an interior step: γ₁‖s_k‖ ≤ γ₁Δ_k. Both satisfy the contraction condition; they differ in how fast the radius falls after a rejection, so which one is in force should be reported rather than left implicit.\n\nRequires η₁ > 0: at η₁ = 0 the aggressive branch is unreachable for every ρ ≥ 0 and the lower-bound constant of Part I degenerates.\n\nwarning: Δmin exists for a reason\nOn an accepted step the new radius is proportional to ‖s_k‖, so a short step gives a small radius, which gives a shorter step still. Near convergence, or when a truncated-CG subsolver stops on its first iteration, this can collapse to zero, after which every step is zero, ρ is NaN, and the solver spins to max_iterations. Δmin floors the radius and turns that silent hang into ordinary slow progress. Set Δmin = 0.0 to reproduce the unguarded rule exactly — which is what the asymptotic claims of Part II are stated about.\n\n\n\n\n\n","title":"TrustRegionRadius.RStep"},{"category":"type","location":"rules/#TrustRegionRadius.RDFO","page":"Radius mechanisms","text":"RDFO(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0, ζ = 1.0, Δmax = Inf)\n\nDFO-like update comparing the radius to the criticality measure:\n\nρ < η₁                 →  Δ ← γ₁ Δ\nρ ≥ η₁ and Δ > ζ‖g_k‖  →  Δ ← γ₂ Δ     (radius large relative to criticality)\nρ ≥ η₁ and Δ ≤ ζ‖g_k‖  →  Δ ← γ₃ Δ     (room to expand)\n\nUses ‖g_k‖ before the accept/reject decision.\n\nDrives Δ_k → 0, so eventual inactivity of the trust region requires ζ > κ̄ = 4/λ*_min(∇²f(x*)). That constant is a property of the solution and cannot be checked in advance; below the threshold the constraint can bind at every iteration, the method converges only linearly, and no first-order diagnostic shows anything wrong. Choose ζ generously: on a heterogeneous test set an over-large ζ costs nothing, while an over-small one costs solved problems.\n\n\n\n\n\n","title":"TrustRegionRadius.RDFO"},{"category":"type","location":"rules/#TrustRegionRadius.RGrad","page":"Radius mechanisms","text":"RGrad(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0, μ = 1.0, half_test = true)\n\nGradient-scaled radius Δ_k = μ_k ‖g_k‖ with an unbounded multiplier:\n\nρ < η₁                     →  μ ← γ₁ μ\nη₁ ≤ ρ < η₂                →  μ ← γ₂ μ\nρ ≥ η₂ and ‖s_k‖ > ½Δ_k    →  μ ← γ₃ μ\notherwise                  →  μ unchanged\n\nHere μ_k is the radius-to-criticality ratio Δ_k/‖g_k‖, so the rule performs a search for the inactivity threshold κ̄ without knowing it: μ climbs on very successful iterations that use the region, contracts otherwise, and stops climbing once the region stops binding. Because μ is uncapped it crosses any threshold eventually, which makes eventual inactivity unconditional — the only rule in the survey for which this holds without a hypothesis on an unknowable constant.\n\nThe middle branch is not decoration: it is what keeps μ from ratcheting upward on mediocre iterations, and the four-branch form is the one the boundedness argument for μ is stated about.\n\nThe guard ‖s_k‖ > ½Δ_k is not cosmetic either. It is what converts \"μ grows\" into \"‖sk‖ is comparable to μk‖gk‖\", which is the step that makes the boundedness argument work. Any constant in (0,1) serves; `halftest = false` disables the guard, which breaks that argument.\n\nSee RGradCapped for the bounded variant required by the global asymptotic theory (Δ_k → 0 needs μ_k ≤ μ̄).\n\n\n\n\n\n","title":"TrustRegionRadius.RGrad"},{"category":"type","location":"rules/#TrustRegionRadius.RGradCapped","page":"Radius mechanisms","text":"RGradCapped(; γ₁ = 0.25, γ₂ = 0.5, γ₃ = 2.0, μ = 1.0, μ_max = 1.0,\n              half_test = true)\n\nRGrad with an explicit cap μ_k ≤ μ_max.\n\nThe cap is what the asymptotic results requiring Δ_k → 0 assume, and it is not free. Eventual inactivity now needs μ_max > κ̄ = 4/λ*_min; below that the trust region binds at every iteration and the method degrades to linear convergence while ρ stays healthy and ‖g‖ keeps falling.\n\nWith a truncated-CG subsolver a small μ_max is worse than slow. The first CG iterate lies along -g, so if the region is small enough that CG truncates there, the returned step is exactly the Cauchy point and the model Hessian stops influencing the search direction: the method has silently become gradient descent, and its limit can be a degenerate critical point that is not a minimiser.\n\n\n\n\n\n","title":"TrustRegionRadius.RGradCapped"},{"category":"type","location":"rules/#TrustRegionRadius.RAdaptiveStep","page":"Radius mechanisms","text":"RAdaptiveStep(; γ₁, γ₂, γ₃, λ₁, λ₂, Δmin = 1e-14)\n\nHei (2003) step-anchored rule with a smooth factor:\n\nΔ_{k+1} = R_{η₁}(ρ_k) · ‖s_k‖\n\nThe switch point of R is the scaling threshold η₁ supplied by the solver, not a constant of the rule, so the family stays comparable with the three-case rules under the same (η, η₁, η₂).\n\nStep-anchored like RStep, so the same Δmin guard applies and for the same reason, and η₁ > 0 is required for the same reason.\n\n\n\n\n\n","title":"TrustRegionRadius.RAdaptiveStep"},{"category":"type","location":"rules/#TrustRegionRadius.RAdaptiveGrad","page":"Radius mechanisms","text":"RAdaptiveGrad(; μ = 1.0, γ₁, γ₂, γ₃, λ₁, λ₂)\n\nHei factor driving a Fan-Yuan multiplier:\n\nμ_{k+1} = μ_k · R_{η₁}(ρ_k),    Δ_{k+1} = μ_{k+1} · ‖g_{k+1}‖\n\nCombines the smooth factor of the Hei family with the ratio-tracking of RGrad: μ is again the radius-to-criticality ratio, but it is scaled continuously in ρ rather than by one of three constants. Because the scaling is multiplicative and R ≤ γ₂ < 1 below η₁, contraction on unsuccessful iterations holds without a guard.\n\n\n\n\n\n","title":"TrustRegionRadius.RAdaptiveGrad"},{"category":"type","location":"rules/#TrustRegionRadius.RRTR","page":"Radius mechanisms","text":"RRTR(; γ₁ = 0.0625, γ₂ = 0.25, γ₃ = 2.5, η̃₁ = 0.05, η̃₂ = 0.9,\n       Δmax = Inf, Δmin = 1e-14)\n\nRetrospective update of Bastin, Malmedy, Mouffe, Toint & Tomanos (2010).\n\nThe radius for iteration k+1 is chosen from how well the new model m_{k+1} retrospectively predicts f at the previous iterate, rather than from how well m_k predicted f at the new one. Acceptance is still decided by ρ and η; only the scaling uses ρ̃.\n\nOn a rejected step (accepted = false), ρ̃ is undefined — it judges a step that was not taken — and the solver passes ρ instead, so the rule branches on the flag first and uses only the sign of the ratio, as the source rule does:\n\nrejected, ρ ≥ 0     →  Δ ← γ₂ ‖s_k‖\nrejected, ρ < 0     →  Δ ← min(γ₂‖s_k‖, γ₁Δ_k)\n\nand on an accepted step\n\nρ̃ ≥ η̃₂             →  Δ ← max(γ₃‖s_k‖, Δ_k)\nη̃₁ ≤ ρ̃ < η̃₂        →  Δ ← Δ_k\n0 ≤ ρ̃ < η̃₁         →  Δ ← γ₂‖s_k‖\nρ̃ < 0              →  Δ ← min(γ₂‖s_k‖, γ₁Δ_k)\n\nThe safeguard factor θ_{k-1} of Part I is taken as 0, so max[γ₁, θ] = γ₁.\n\nBranching on accepted is mandatory, not defensive. Comparing a classical ρ against the retrospective thresholds would leave the radius untouched for ρ ∈ [η̃₁, η) — an unsuccessful iteration with no contraction, which forfeits weak admissibility and, since a rejected step changes neither the model nor the iterate, reproduces the same subproblem for ever.\n\nThe first accepted branch is the interesting one: max(γ₃‖s_k‖, Δ_k) is simultaneously step-driven and non-decreasing, which is what lets this rule secure eventual inactivity from a consequence of the standing assumptions (ρ̃ → 1) rather than from a condition on a user parameter, as RDFO needs. The price is structural: ρ̃ → 1 requires either the secant condition on the model, or asymptotic second-order coherence plus a quadratic model decrease.\n\n\n\n\n\n","title":"TrustRegionRadius.RRTR"},{"category":"type","location":"rules/#TrustRegionRadius.RRTRGrad","page":"Radius mechanisms","text":"RRTRGrad(; γ₁ = 0.25, γ₃ = 2.0, μ = 1.0, η̃₁ = 0.05, η̃₂ = 0.9,\n           half_test = true)\n\nRetrospective gradient-scaled rule of Fan, Pan & Song (2016):\n\nμ_{k+1} = γ₁ μ_k     if the step was rejected, or ρ̃ < η̃₁\n          γ₃ μ_k     if ρ̃ ≥ η̃₂ and ‖s_k‖ > ½Δ_k\n          μ_k        otherwise\nΔ_{k+1} = μ_{k+1} ‖g_{k+1}‖\n\nStructurally RGrad with ρ replaced by ρ̃ and no intermediate contraction, so γ₂ is absent from this rule. The argument that μ cannot diverge is ratio-agnostic — it uses only the monotone decrease of f, the local quadratic bounds, and the ½Δ test — so it transfers unchanged from RGrad to this rule. All the ratio has to supply is convergence to 1.\n\nThe explicit rejected branch matters for the same reason as in RRTR: on a rejected step ‖g_{k+1}‖ = ‖g_k‖, so leaving μ untouched returns exactly Δ_k and the solver cannot make progress.\n\n\n\n\n\n","title":"TrustRegionRadius.RRTRGrad"},{"category":"function","location":"rules/#TrustRegionRadius.initial_radius","page":"Radius mechanisms","text":"initial_radius(rule, Δ₀, g_norm) -> Float64\n\nRadius at iteration 0. Most rules return Δ₀; those of the form Δ = μ‖g‖ return μ₀ · g_norm and so ignore Δ₀ entirely.\n\n\n\n\n\n","title":"TrustRegionRadius.initial_radius"},{"category":"function","location":"rules/#TrustRegionRadius.update_radius!","page":"Radius mechanisms","text":"update_radius!(rule, Δ, ρ, accepted, η₁, η₂, s_norm, g_norm_old, g_norm_new)\n\nRadius for the next iteration. May mutate rule (e.g. the multiplier μ). See RadiusRule for the meaning of each argument.\n\n\n\n\n\n","title":"TrustRegionRadius.update_radius!"},{"category":"function","location":"rules/#TrustRegionRadius.reset_rule!","page":"Radius mechanisms","text":"reset_rule!(rule) -> nothing\n\nRestore mutable state to its construction value. Default: no-op. Called at the start of every solve and by SolverCore.reset!.\n\n\n\n\n\n","title":"TrustRegionRadius.reset_rule!"},{"category":"function","location":"rules/#TrustRegionRadius.needs_retrospective","page":"Radius mechanisms","text":"needs_retrospective(rule) -> Bool\n\ntrue if the rule expects the retrospective ratio ρ̃ in the ρ slot of update_radius!. The solver computes ρ̃ only when this returns true, since it costs one extra model evaluation at the previous iterate, and only on accepted steps, since ρ̃ compares the new model against a step that was taken.\n\n\n\n\n\n","title":"TrustRegionRadius.needs_retrospective"},{"category":"function","location":"rules/#TrustRegionRadius.is_criticality_anchored","page":"Radius mechanisms","text":"is_criticality_anchored(rule) -> Bool\n\ntrue if the radius is tied to a criticality measure, so that Δ_k → 0 along a convergent run. For these rules eventual inactivity of the trust region requires a parameter above the problem-dependent threshold κ̄ = 4/λ*_min.\n\nThe complement is not a single regime: RDelta keeps liminf Δ_k > 0, while the step-anchored rules drive Δ_k → 0 in the local regime with the stronger summability Σ_k Δ_k < ∞, inherited from Powell's theorem on the step series. Use asymptotic_regime when the three-way distinction matters.\n\n\n\n\n\n","title":"TrustRegionRadius.is_criticality_anchored"},{"category":"function","location":"rules/#TrustRegionRadius.asymptotic_regime","page":"Radius mechanisms","text":"asymptotic_regime(rule) -> Symbol\n\nWhich of the three asymptotic regimes the rule belongs to:\n\n:vanishing      — Δ_k → 0 with Σ_k Δ_k²/M_k < ∞ (criticality-anchored);\n:step_summable  — Σ_k Δ_k < ∞ in the local regime (step-anchored);\n:bounded_below  — liminf_k Δ_k > 0 (criticality-blind, radius-anchored).\n\n\n\n\n\n","title":"TrustRegionRadius.asymptotic_regime"},{"category":"function","location":"rules/#TrustRegionRadius.retrospective_ratio","page":"Radius mechanisms","text":"retrospective_ratio(actual, s, g_new, Hnew_s) -> Float64\n\nRetrospective ratio ρ̃, from\n\nactual: f(x_k) − f(x_{k+1}), the achieved reduction (same numerator as ρ);\ns:      the step s_k;\ng_new:  g_{k+1}, the gradient at the new iterate;\nHnew_s: H_{k+1} · s_k, the new model Hessian applied to the step.\n\nReturns -Inf when the retrospective predicted reduction is non-positive, which sends the rule to its most conservative branch — the same convention the solver uses for ρ.\n\nThe denominator is −g_{k+1}ᵀs_k + ½ s_kᵀH_{k+1}s_k. Note the sign of the first term: it is a reduction measured backwards along the step, so with g_{k+1} roughly parallel to s_k near convergence the term is positive.\n\n\n\n\n\n","title":"TrustRegionRadius.retrospective_ratio"}]
}
